{
  "name": "metro-tdx-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for TDX Metro LiveBoard with zero client key exposure and edge caching",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
